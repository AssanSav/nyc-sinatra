<form action="/figures" method="POST">
    <h2>Name:<h2> 
    <input type="text" name="figure[name]" id="figure_name">

    <h2>Titles</h2>
    <% Title.all.each do |title| %> 
    <div>
        <label><%= title.name %></label>
        <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>" >
    </div>
    <% end %>
    <br>
   
    <h2>Create New Title:<h2>
    <input type="text" name="title[name]" id="new_title">
    
    <h2>Landmaks</h2>
    <% Landmark.all.each do |landmark| %> 
    <div>
        <label><%= landmark.name %></label>
        <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%= landmark.id %>">
    </div>
    <% end %>
    
    
    <h2>Create New Landmark:</h2>
    <label><strong> Name: </strong></label><input type="text" name="landmark[name]" id="new_landmark">
    <label><strong> Year: </strong></label><input type="text" name="landmark[year]" id="new_landmark"><input type="submit" value="Create New Figure">

</form>